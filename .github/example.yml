name: Self Hosted Demo

on:
  workflow_dispatch:

jobs:
  plan:
    runs-on: self-hosted

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Print index.html
        run: cat index.html
